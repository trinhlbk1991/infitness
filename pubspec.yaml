name: infitness
description: Simple workout planner and timer app crafted using Flutter

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+110

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  bloc: ^8.0.1
  flutter_bloc: ^8.0.0
  provider: ^6.0.0

  hive: ^2.0.4
  hive_flutter: ^1.1.0

  google_fonts: ^2.0.0
  animated_bottom_navigation_bar: ^0.3.2
  expandable: ^5.0.1
  dotted_border: ^2.0.0
  group_button: ^3.3.1
  simple_timer: ^2.0.0
  flutter_slidable:
  fluttertoast: ^8.0.8

  equatable: ^2.0.3
  tuple: ^2.0.0
  collection: ^1.15.0
  uuid: ^3.0.2
  intl: ^0.17.0

  path: ^1.8.0
  path_provider: ^2.0.5
  connectivity: ^3.0.3
  package_info:
  fimber_io: ^0.6.0
  flutter_tts: ^3.2.2
  assets_audio_player: ^3.0.3+1
  wakelock: ^0.5.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  hive_generator: ^1.1.1
  build_runner: ^2.1.4

flutter:
  uses-material-design: true
  assets:
    - assets/
